# CHANGELOG

Формат: `[YYYY-MM-DD]` — опис змін (зосереджено на «чому», не лише «що»).

---

## [2025-06-24]

### docs/ залишається в sim-docaa (не окрема папка поруч)
- Документація в `docs/` всередині репо; `deploy.sh` виключає її на сервері

### Структура docs/ — документація всередині репо
- Документація в `docs/` (не на сервер)
- `deploy.sh` виключає всю папку `docs/`

### Деплой: розділення dev / prod
- `scripts/deploy.sh` — rsync лише runtime (без `docs/`, data/, .env)
- `.gitignore` — node_modules, data, .env

### Система пам'яті проєкту
- Додано `AI_CONTEXT.md`, `DECISIONS.md`, `ROADMAP.md`, `BUSINESS_RULES.md`, `CHANGELOG.md` для збереження контексту між сесіями AI/Cursor.

### Фікс торгу через біржу
- `migrate-v26`: колонка `cargo_board.letter_id` для прив'язки поста до рейсу
- Форма біржі: обов'язковий вибір рейсу зі списку, автозаповнення полів
- Сервер: прибрано лазівку фільтру напрямку (`dirsNeeded.length === 0 ||`)
- `server/utils/route-matcher.js`: покращений матчинг міст (аліаси, кирилиця/латиниця)
- `matchOrderByRoute`: ті самі аліаси на фронті; поріг score ≥ 2
- Біржовий відгук: закріплення чату за `letter_id` поста (`chatLockedLetter` + PATCH lock)
- `freight_ref` з `letters_v2` на сервері при підборі біржових перевізників

---

## [2025-06-19] — з git (останній коміт до цієї сесії)

### fix: торг бере ref конкретного рейсу (54a4c0b)
- Варіант 2: рейс визначається за маршрутом з повідомлення студента і закріплюється за чатом (`locked_letter_id`).

### feat: детермінований торг B (b8af649)
- Ціну рахує код; стан торгу в БД; межі ref±300/100.

### fix: біржові перевізники тримають межі (aca92da)
- freightRef доходить до промпту біржових перевізників.

---

_Повна хронологія деплоїв: `деталі проекту з чату claude/docaa_handoff/01_ХРОНОЛОГІЯ_ДЕПЛОЇВ.md`_
